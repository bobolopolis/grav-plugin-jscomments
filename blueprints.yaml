name: JSComments Plugin
version: 1.0.6
description: Add comments in your site with Disqus / IntenseDebate / Facebook / Muut Javascript system.

form:
  fields:
    enabled:
      type: toggle
      label: Plugin status
      highlight: 1
      default: 0
      options:
        1: Enabled
        0: Disabled
      validate:
        type: bool
